function updatePage(buttonUpdate) {
  const updateForms = document.querySelectorAll('form');
  updateForms.forEach((updateForm) => {
    updateForm.addEventListener('click', function (event) {
      event.preventDefault();

      // console.log('dd');
    });
  });
}
const buttonUpdate = () => {};
// updatePage();
